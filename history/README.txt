HISTORY — archived planning docs (kept for reference, not for active use)
========================================================================
Don't re-read these to plan new work; the live roadmap is ../next_steps_2.txt.
These are snapshots of completed roadmaps, parked here so day-to-day planning
doesn't pay the token cost of re-reading finished passes.

  next_steps_post-v1.8_DONE.txt
    The post-v1.8 roadmap (created 2026-06-15). ALL FIVE passes shipped:
      PASS 1  Progression / Career ladder ........ v1.9
      PASS 2  PGN export-import + Puzzle mode ..... v2.0
      PASS 3  Accessibility + Cinzel font ........ v2.1
      PASS 4  Opening book + transposition table . v2.2
      PASS 5  Trailer reel + capture pipeline .... v2.3
    Plus an unplanned extra: Classic 2D board view (v2.4).
    The only items NOT closed were two playtest-only validations (real-network
    online test; real mobile-device test) — those are carried forward into
    next_steps_2.txt so nothing is lost.
</content>
